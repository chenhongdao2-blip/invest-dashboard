"""Locale tables for the dashboard i18n layer (see lib/i18n.py).

Each module exposes a flat `STRINGS: dict[str, str]` keyed by dotted namespaces
(e.g. 'strategy.page.title'). Plain module-level dicts — never cached.
"""
