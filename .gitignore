# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/
.DS_Store

# Streamlit secrets (keep local, not commit)
.streamlit/secrets.toml

# logs / scratch
*.log
.scratch/
tmp/

# uv
.uv-cache/

# OMC plugin auto-state (do not commit)
.omc/
.claude/

# B1 audit fix: don't commit raw ic-foundry ledger (contains IP — thesis/conviction/postmortem JSON)
data/external/picks.db
data/external/picks.db-shm
data/external/picks.db-wal
data/external/*.db
data/external/*.db-shm
data/external/*.db-wal

# local CSV backups (never commit)
data/external/_bak_*/
