# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/
.DS_Store

# Streamlit secrets (keep local, not commit)
.streamlit/secrets.toml

# logs / scratch
*.log
.scratch/
tmp/

# uv
.uv-cache/

# OMC plugin auto-state (do not commit)
.omc/
.claude/

# B1 audit fix: don't commit raw ic-foundry ledger (contains IP — thesis/conviction/postmortem JSON)
data/external/picks.db
data/external/picks.db-shm
data/external/picks.db-wal
data/external/*.db
data/external/*.db-shm
data/external/*.db-wal

# local CSV backups (never commit)
data/external/_bak_*/

# stray / backup / temp — never commit (added 2026-06-01)
/snapshots.db
data/snapshots.db.bak-*
cov_*.txt
cov_*.json

# Analyst model extracts — FULL versions are confidential (TP/DCF + Visible Alpha):
# local-only, NEVER public Cloud. The desensitized copies under data/models_public/
# (dcf/target stripped by jobs/export_public_model.py) ARE committed & shipped to Cloud.
data/models/*.json
!data/models_public/

# China-fund HC positioning — FULL files carry real fund names (confidential):
# local-only, NEVER public Cloud. The committed china_fund_hc_positioning.csv is the
# ANONYMISED (Fund 1–12) copy emitted by jobs/build_hc_overview_data.py.
data/external/china_fund_hc_positioning_full.csv
data/external/china_fund_hc_positioning_source_full.txt

# HC positioning runbook references real fund names (gotchas + leak-scan tokens) — local-only.
docs/hc-positioning-pipeline.md

# Healthcare headcount/relative-perf standalone deliverables (regenerable via jobs/*.py; app uses the CSV)
data/external/cn_pharma_headcount_2025.png
data/external/cn_pharma_headcount_2025.xlsx
data/external/hc_relative_performance.xlsx
~$*.xlsx
data/external/hshci_history.png
data/external/hshci_drawdown_compare.png
落地代码/
