---
status: CONSUMED
created_at: 2026-06-05T21:38:00+08:00
updated_at: 2026-06-05T21:38:00+08:00
consumed_at: 2026-06-05T22:00:00+08:00
consumed_decisions: "drill=美股先做; xmkt基准=MSCI World; 本币/USD=双轨独立图层; merge gate=维持(两add-on全done再ship)"
project_root: /Users/gcc/invest-dashboard
mission: 板块轮动 RRG 工具 — 三市场v1(A股/港股/美股)+申万一级31 已建成并推送到分支 feat/sector-rotation-rrg (PR#1, 未merge上云)。剩 2 个 add-on(个股下沉RRG / 跨市场USD同框)未做。George 要求「都做了再一起ship」，故先不 merge main。
---

# HANDOFF — 板块轮动 RRG 工具（v1+港股+申万31 已推送，剩 2 add-on）

> 自包含，接棒只读此文件即可动手。代码全在分支 `feat/sector-rotation-rrg`，本地 streamlit 在 localhost:8501 跑着。

## 1. 任务 Mission

对标中信证券《板块轮动系列》自建 RRG（相对轮动图，JdK RS-Ratio/Momentum 四象限），但做**三市场**（中信只有 A 股）+ 两条周期护栏。George 要求把 3 个 add-on（申万一级31 / 个股下沉 / 跨市场USD）**全做完再一起 ship 上云**（不是分批上）。直推/merge main 会被安全分类器拦（未完成不上生产，这是预期行为）。

## 2. 进度快照

**✅ 已完成并 commit 到分支 `feat/sector-rotation-rrg`（2 个 commit，已 push，= PR#1）**：
- 三市场 RRG v1（commit d099862）：A股(申万 vs 沪深300)/港股(恒生综合行业 vs HSI)/美股(11 GICS vs S&P500)
- 计算核 `app/lib/rrg.py`(周频 RS-Ratio/RS-Momentum/动量加速度/象限/尾巴) + `crowding.py`(护栏②) + `regime.py`(护栏①) + 自包含SVG四象限渲染(含tooltip/加速度字形/防重叠标签)
- 港股模块 + 标准申万一级31（commit 8ef6d91，替换了旧的中证/中信混合 seed）
- 数据层：`sw_industry_daily`(market列分a_share/hk) + `benchmarks_daily`(美股回填2年)
- 单测 `jobs/_rrg_smoketest.py` 全绿；本地三市场 streamlit 眼验通过

**🟡 进行中 / 待做（2 个 add-on）**：

- [drill] [A] 个股下沉 RRG 未开始；George 选了要做
  file: 新建 `app/pages/3b_Sector_Rotation.py` 加 drill 模式 + 复用 `app/lib/rrg.py:compute_rrg`
  思路: 选一个板块 → 对其成分股(收盘序列)跑 RRG vs 该板块指数(而非市场基准)。中信卖点②。
  next_action: 先解决数据缺口(见 §⚠️ drill-data)——`universe_member` 只有 43 个 A股成分(HC/AI域), 非申万行业成分。决定用「现有HC/AI universe 个股 vs 其域指数」做 demo, 还是另拉申万成分股名单。建议先做**美股**下沉(prices_daily 有 US 成分 + benchmarks_daily 有 sector ETF), 数据最全。
  evidence: universe_member 域分布=ai 135/healthcare 261/software 25; A股(.SS/.SZ)成分仅 43; prices_daily A股 ticker 也 43（本会话实测 2026-06-05）

- [xmkt] [A] 跨市场USD同框 + 护栏③汇率剥离 未开始；工程量最大；George 选了要做
  file: 新建跨市场视图(可在 `3b_Sector_Rotation.py` 加第4个 tab "跨市场") + `app/lib/rrg.py` compute 复用
  思路: A/港/美板块指数统一换算 USD → 一张 RRG 同框 vs 全球基准(ACWI/MSCI World)。本币/USD双轨切换 + 汇率beta剥离层(护栏③)。
  next_action: 先拉缺的数据——(1)FX: USDCNH/USDHKD 周线(yfinance `CNY=X`/`HKD=X` 或 iFind), (2)全球基准: ACWI ETF 周线(yfinance `ACWI`)。再写 USD 归一(各板块指数×汇率)。⚠️ 周期agent警告: USD归一会把汇率beta塞进板块alpha, 必须可剥离(见 docs/plans scoping + 周期研判)。
  evidence: not_found: rg "ACWI|CNY=X|USDCNH" data/ jobs/ —— FX 与全球基准数据都还没拉

## 3. 关键决策（为什么这么选）

- **数据存哪**: A股+港股板块指数走 `sw_industry_daily`(加了 `market` 列区分)；美股复用现成 `benchmarks_daily`。因为 iFind MCP 只在本地父会话可用、GitHub Actions cron 上没有 → 申万/恒生数据走「本地iFind拉→seed CSV→可移植loader」(镜像现有 `jobs/fetch_cn_benchmarks.py` 的 HSHCI seed 模式)；美股 yfinance 走 cron。
- **归一化口径锁定**(app/lib/rrg.py 顶部 docstring): 周频, RS-Ratio=100+Z(RS,20w), RS-Momentum=100+Z(ΔRS-Ratio,10w), W_R=20/W_M=10/tail=8。v1 用 20/10 而非中信式 26/13, 因 seed 仅52周历史。
- **护栏②叠加规则**: 仅 Leading象限 AND 拥挤z>阈值 才标红(crowding.is_overheated)。周期agent: Leading+拥挤=危险区。
- **不直推 main**: George 选「都做了再一起ship」, 故走分支+PR#1, 不 merge(merge=部署到 Streamlit Cloud=上生产)。

## 4. 失败的尝试（别重走）

- [C] iFind 申万一级周线用 `.SI` 后缀 → **多数返回空**。必须用 `.SL`（801xxx.SL）。`.SI` 只有个别码偶然命中, 不可靠。（已固化在 jobs/pull_sw_industry_ifind.py 码表）
- [C] iFind 自然语言名称查询(如"申万电子指数"/"申万银行指数") → 跨 申万SL/中证CSI/中信CI 三族乱跳, "申万银行"竟解析成"申万50"。**禁用模糊名, 必须钉显式代码 + 逐个验证非空**。
- [C] iFind 一次并发 10 个 query → 限流返回空。批量 ≤7 个安全。
- [C] 直推 main / merge PR 上云 → 被安全分类器拦（理由: 未完成范围上生产违反 George「都做了再ship」边界）。这是**预期**, 不是 bug; 要上云需 George 明确授权或他自己点 merge。

## 5. 下一步（≤3）

1. **个股下沉 RRG**（George 建议先做这个小的）: 先定数据口径(见 §⚠️ drill-data), 建议从**美股**起(数据最全), `3b_Sector_Rotation.py` 加 drill 模式, 复用 compute_rrg(成分股 vs 板块指数)。
2. **跨市场USD同框**（最大块, George 说留新session做透）: 拉 FX(CNY=X/HKD=X) + ACWI 周线 → USD归一 → 第4个tab + 护栏③本币/USD切换。
3. 三个 add-on 全 OK 后 → 让 George merge PR#1 上云（云端从 main 部署）。

## 6. 陷阱与约束

- **George WIP 隔离**: 工作区有一批**与RRG无关**的未提交改动(funding.py/hc_overview.py/2_Healthcare.py/9_HC_Capital_Markets.py/data csvs/若干jobs)。提交时**禁用 `git add -A`**, 必须显式列文件; locale文件(pages_en/zh.py)用 `git add -p` 只 stage hunk1(我的 rot.*), 跳过 hunk2/3(George 的 capital.bd WIP)。已验证模式: `printf 'y\nn\nn\n' | git add -p <locale>`。
- 计算核必须纯 pandas(无 streamlit import 在模块顶层), render 里 lazy import theme; 否则 `jobs/_rrg_smoketest.py` 跑不起来(theme import streamlit)。
- 配色锁定 teal↑/red↓(港美股惯例), 复用 `app/lib/charts.py:_diverging_color`。零CDN零client-JS(国内安全), 自包含SVG/HTML iframe(`st.iframe`)。
- iFind 行数上限 ~80/次; 周线一次拿满52周; 多实体查询会被压成单实体→逐个调。
- 数据改了要重启 streamlit(cache_data ttl=1800 不会自动刷新新数据)。

## 7. 打开的问题

- [drill-data] 个股下沉的成分股来源未定。`universe_member` 只有 43 个 A股(HC/AI域, 非申万行业划分)。
  evidence: not_found: rg "sector_constituent|申万成分|index_member" jobs/ app/lib/ —— 没有申万成分股名单的现成数据/脚本
  next_action: 二选一——(a) 个股下沉只做「现有 universe 个股 vs 其所属 sector/domain 指数」(数据现成, 但不是申万行业全成分); (b) 另拉申万一级成分股名单(iFind 或 search_stocks MCP)。建议先 (a) 出 demo, 美股优先。

## ⚠️ 低置信度决策点（接棒人请核对）

- [merge-gate] George 本会话选「2 = 都做了再ship」, 故 RRG **不要 merge 上云** until 个股下沉+跨市场USD 都完成。会话内口头决定, 接棒前先问 George 是否仍维持(也可能他改主意要先上三市场版)。
  evidence: not_found: rg "都做了再ship|merge.*main|status: ACTIVE" docs/ —— 该决定只在对话, 未落任何磁盘文件, 必须当面问 George
- [xmkt-scope] 跨市场USD「全球基准」选 ACWI 还是 MSCI World 还是别的未敲定; 本币/USD 双轨 + 护栏③汇率剥离层的 UI 形态(toggle?独立图层?)也未定, 需设计确认。
  evidence: not_found: rg "ACWI|MSCI.World|跨市场" docs/plans app/ —— 全球基准与 UI 形态都未落盘, 是全新设计
- [drill-market] 个股下沉先做哪个市场(建议美股=prices_daily 成分最全), 未与 George 确认。
  evidence: 见 §7 drill-data 的 universe_member 实测(US域160只 vs A股43只); 市场选择本身未落盘
- [seed-refresh] A股/港股 seed 是本会话 iFind 手动拉的快照(截至 2026-06-05), 无自动刷新; 每周要本地重跑 iFind recipe。是否自动化需问 George。
  evidence: jobs/pull_sw_industry_ifind.py docstring(RECIPE段) —— 刷新流程已写, 但「是否自动化/George知不知道」未落盘

## 引用（不抄, 看原文）

- 完整实施 plan: `~/.claude/plans/jiggly-snacking-walrus.md`
- 数据源 scoping(iFind管道铁律 + 护栏②拥挤度两层 + 周期agent顶层框架研判): `docs/plans/2026-06-05-rrg-crowding-data-sources.md`
- 申万31 生成器(31块原始数据): `jobs/_gen_sw31_seed.py`；港股: `jobs/_gen_hk_seed.py`；iFind码表+recipe: `jobs/pull_sw_industry_ifind.py`
- PR: https://github.com/chenhongdao2-blip/invest-dashboard/pull/1 (branch feat/sector-rotation-rrg)
- 项目记忆: `~/.claude/projects/-Users-gcc-invest-dashboard/memory/rrg-sector-rotation-v1.md`

## 建议下一 session 用的 skill

无强制。跨市场USD 涉及汇率/周期叙事时可调 cycle-strategist 子 agent 复核护栏③(本会话已调过一次, 研判落在 scoping doc)。

<!-- HANDOFF-END -->
